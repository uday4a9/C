#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main()
{
    int i = -1;
    ssize_t s = -1;

    printf("Existed i : %d, s : %u\n", i, s);
    printf("%d \n %u \n", -1, -1);

    return 0;
}
